# Core dependencies
torch>=2.1.0
fastapi
uvicorn
pillow
trimesh
PyYAML>=6.0

# Hugging Face dependencies
diffusers
transformers

# Additional dependencies
numpy
rembg

# Specify an older version of onnxruntime that's compatible
onnxruntime==1.15.1

# For pymeshlab, we need to install it via conda
# Since we can't install it via pip in this environment

# Demo only
torch>=2.1.0
trimesh
Pillow
fastapi
uvicorn
numpy
rembg
onnxruntime
#gevent
#geventhttpclient
PyYAML>=6.0

# Added from the code block
pymeshlab